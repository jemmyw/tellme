# -*- ruby -*-

require 'rubygems'
require 'hoe'
require './lib/tellme.rb'

Hoe.new('tellme', Tellme::VERSION) do |p|
  # p.rubyforge_name = 'tellmex' # if different than lowercase project name
  p.developer('Jeremy Wells', 'jeremy@boost.co.nz')
  p.extra_deps         = [
    ['sparklines','>= 0.5.2'],
    ['chronic', '>= 0.2.3'],
    ['mechanize', '>= 0.8.4'],
    ['hpricot', '>= 0.6.161']
  ]
end

# vim: syntax=Ruby
